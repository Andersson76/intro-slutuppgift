name: CI Check

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Visa meddelande
        run: echo 'Hej! Detta är en automatisk körning via GitHub Actions'
